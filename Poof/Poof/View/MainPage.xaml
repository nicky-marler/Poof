<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Poof"
             x:Class="Poof.MainPage">
    <StackLayout>
        <Entry Text="{Binding Testy.Name}"
               Placeholder="Some person(or you)"/>
        
        <Entry Text="{Binding Testy.Other_Name}"
               Placeholder="Another person"/>
        
        <Label Text="{Binding Testy.Statement}" 
           VerticalOptions="Center" 
           HorizontalOptions="Center" />

        <Button Text="Reset" 
                Command="{Binding Reset_Command}"/>
    </StackLayout>
</ContentPage>